{
  "name": "Opencode",
  "image": "oven/bun:1.3.14",
  "onCreateCommand": "apt update && apt upgrade -y && apt install -y git python3 build-essential",
  "postCreateCommand": "git config --global --add safe.directory \"$(pwd)\""
}